version: "3.8"
services:
  name_recognition:
    build: 
      context: .
      target: name_recognition
    networks:
      nr_networks:
    ports:
      - "5000:5000"
  python:
    build: 
      context: .
      target: test
    networks: 
      nr_networks:

networks:
  nr_networks:
    driver: bridge